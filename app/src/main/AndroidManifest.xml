<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".main.MainApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.CaseManagementApp"
        tools:targetApi="34">

        <!-- 🔑 Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBkfcAJz0ELEAcND5jstBDT2duDPlvmkFk"/>

        <activity
            android:name=".main.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.CaseManagementApp.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".main.LoginActivity" />
        <activity android:name=".views.caseslist.CaseListView" />
        <activity android:name=".views.case.CaseView" />

        <!-- Map screen -->
        <activity
            android:name=".views.map.MapView"
            android:exported="false" />

        <activity
            android:name=".views.map.MapViewActivity"
            android:exported="false" />


    </application>

</manifest>
